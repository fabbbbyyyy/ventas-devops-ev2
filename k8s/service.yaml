apiVersion: v1
kind: Service
metadata:
  name: backend2-service
spec:
  selector:
    app: backend2
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP   # solo accesible dentro del cluster (frontend le habla por este nombre)
